# Markdown 手测样例

把下面内容作为一条消息发送，或让 agent 原样回复这份文件的内容。

## 表格、对齐和横向滚动

| Agent | 状态（居中） | 数量（右对齐） | 长说明 | 下载 |
| :--- | :---: | ---: | :--- | :--- |
| Pi | **就绪** | 12 | 这是一段需要自动换行的长内容，应该完整显示到这里：结束。 | [本文件][sample] |
| Codex | *就绪* | 1234 | 单元格中的转义竖线：a\|b；代码：`x\|y`。 | [网页](https://example.com) |

表格应该能左右滑动，末列链接应该能点击。表格下面这段正文应保持正常宽度。

## 嵌套列表和任务

7. 从七开始的有序列表。
   - 嵌套第一项。
   - [x] 已完成任务。
   - [ ] 未完成任务。

   > 引用中的 **粗体** 与 [网页链接](https://example.com)。
   >
   > 引用的第二段。

8. 下一项应显示八。

## 行内格式与换行

**粗体包含 *斜体* 和 [链接](https://example.com)**，~~删除线~~，`foo_bar`，普通变量 foo_bar_baz，转义 \*星号\*，实体 &amp;。

这一行结尾有两个空格。  
这里应强制换行。<br>这里也应换行。

## 代码原文、横向滚动与复制

````markdown
```kotlin
val message = "这是一条很长很长很长很长很长很长很长很长的代码行，应该可以横向滚动查看末尾"
**这里不应该变成粗体**
[这里不应该成为行内可点击链接](/tmp/example.txt)
```
````

    四个空格缩进的代码块。
    保留 **原始标记**。

## 链接和图片

自动网址：https://example.com

引用式文件链接：[下载这份样例][sample]。

![远程图片示例](https://www.python.org/static/community_logos/python-logo.png)

图片无法加载时应保留可点击的替代文字，不影响其余消息。

[sample]: <D:/pi-remote-markdown/android/docs/markdown-smoke.md>
